name: 'my_duckdb_project'
version: '1.0.0'
config-version: 2

profile: 'duckdb_minio_profile' 

# Пути к папкам проекта
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  my_duckdb_project:
    example:
      +materialized: view 